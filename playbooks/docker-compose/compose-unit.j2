[Unit]
Description=test docker compose unit
After=docker.service

[Service]
WorkingDirectory={{ item.root_dir }}
ExecStart=docker compose -f {{ item.root_dir }}/docker-compose.yml up
ExecStop=docker compose -f {{ item.root_dir }}/docker-compose.yml down

[Install]
WantedBy=docker.service